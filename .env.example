# Copy to .env and adjust as needed
PORT=5000
JWT_SECRET=change-me-to-a-long-random-string
# DB_PATH=./data.sqlite

# AI assistant ("Ask your books") — get a key at https://console.groq.com
# Leave unset to disable the AI chat (the app works fine without it).
# GROQ_API_KEY=gsk_...
# GROQ_MODEL=llama-3.3-70b-versatile
# AI_DAILY_LIMIT=50
